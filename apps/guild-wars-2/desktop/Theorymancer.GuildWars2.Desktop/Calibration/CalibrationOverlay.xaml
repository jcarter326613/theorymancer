<Window x:Class="Theorymancer.GuildWars2.Desktop.Calibration.CalibrationOverlay"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#01000000"
        Topmost="True"
        ShowInTaskbar="False"
        KeyDown="OnKeyDown">
  <Grid>
    <Canvas x:Name="OverlayCanvas"
           Background="#01000000"
           Cursor="Cross"
           MouseLeftButtonDown="OnMouseLeftButtonDown"
           MouseMove="OnMouseMove"
           MouseLeftButtonUp="OnMouseLeftButtonUp"
           MouseRightButtonDown="OnMouseRightButtonDown" />
    <Border HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20" Padding="12" Background="#D9111111" CornerRadius="4" IsHitTestVisible="False">
      <TextBlock Foreground="White" Text="Drag empty space to create a region. Drag a region to move it. Right-click a region to delete it. Confirm or cancel from the collector window." />
    </Border>
  </Grid>
</Window>
